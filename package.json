{
  "name": "auto-threads",
  "version": "1.0.0",
  "description": "Threads 完全自動投稿（本文=OpenAI生成 + テキスト中心/朝だけPexels写真 + GitHub Actions）",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "generate": "node src/generate.mjs",
    "post": "node src/post.mjs",
    "refresh-token": "node src/refreshToken.mjs",
    "run": "node src/generate.mjs && node src/post.mjs"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "openai": "^4.104.0"
  }
}
